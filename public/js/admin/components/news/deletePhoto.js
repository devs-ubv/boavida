$(document).ready(function () {

    /* var idImageDelete = $('#deleteImage').data('content');
    if(idImageDelete){ */
   /*  } */

    $('#delete-image').on('click', function () {
       
            // Eliminar dados da API
            /* $.ajax({
                url: `/upload/${idDeletete}`, 
                method: 'DELETE', 
                success: function (response) {
                    console.log("Imagem eliminada com sucesso!!!", response);
                    alert("Imagem Eliminada com sucesso!!!");
                },
                error: function (error) {
                    console.error('Erro ao eliminar imagem da API: ' + error.statusText);
                    alert("Falha ao eliminar a imagem!!!");
                }
            }); */
            console.log("Ola Mundo");
        
    });

});